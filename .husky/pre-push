#!/bin/sh
echo "Running pre-push checks..."
npm run build || { echo "BUILD FAILED - push rejected"; exit 1; }
npm run test || { echo "TESTS FAILED - push rejected"; exit 1; }
echo "All checks passed!"
echo "NOTE: Full coverage check runs in CI"
