#!/bin/sh
echo "Running build + tests with 90% coverage..."
npm run build || { echo "BUILD FAILED - push rejected"; exit 1; }
npm run test:coverage || { echo "TESTS FAILED or COVERAGE BELOW 90% - push rejected"; exit 1; }
echo "All checks passed!"
